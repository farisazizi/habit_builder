<h1>Make your habit sounds stronger !</h1>

<div class="row">
    <div class="col-md-6 col-md-offset-1">
        <%= form_for(@habit) do |f| %>
            <%= render 'habits/form', f: f %>
            <br></br>
            <%= f.submit "Ready to go !", class: "btn btn-primary" %>
        <% end %>
        <br></br>
        <br></br>
        <%= link_to 'Show', @habit %> |
        <%= link_to 'Back', root_url %>
    </div>
</div>


